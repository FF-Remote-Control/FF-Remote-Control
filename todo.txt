- Menu item in addition to a toolbar button?
- Auto-install toolbar button (see urls.txt)

Posts:
- evalInSandbox
    - Relative urls. In ajax requests, but also e.g.
      "window.location='relative.html'".
    - foo={a:1, b:2} doesn't "stick", but foo.a=345 and window.foo=43 do?
- Firebug
    - Calling Firebug.Console.log - is that a security problem?
    - How do I make sure that log messages go to the right window?
